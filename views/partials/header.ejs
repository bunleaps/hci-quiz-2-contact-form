<div class="topnav">
  <h1 class="logo">Hello 👋</h1>
  <ul class="navlinks">
    <a href="/"
      ><li class="<%= currentPage === 'home' ? 'active' : '' %>">🏠 Home</li></a
    >
    <a href="/education"
      ><li class="<%= currentPage === 'edu' ? 'active' : '' %>">
        🏫 Education
      </li></a
    >
    <a href="/skills"
      ><li class="<%= currentPage === 'skills' ? 'active' : '' %>">
        💼 Skills
      </li></a
    >
    <a href="/contact"
      ><li class="<%= currentPage === 'contact' ? 'active' : '' %>">
        📨 Contact
      </li></a
    >
  </ul>
</div>

<script>
  $(document).ready(function () {
    $("a").hover(function () {
      $(this).addClass("animation-haha");
    });
  });
</script>

<style>
  .topnav {
    width: 900px;
    height: 60px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    /* border-bottom: 3px solid #1dd1a1; */
  }

  .topnav ul {
    display: flex;
  }

  .topnav li {
    margin: 10px;
    list-style-type: none;
    font-size: 20px;
    cursor: pointer;
  }

  li:hover {
    color: #1dd1a1;
  }

  .active {
    color: #1dd1a1;
  }

  .animation-haha:hover {
    /* Start the shake animation and make the animation last for 0.5 seconds */
    animation: shake 0.5s;

    /* When the animation is finished, start again */
    animation-iteration-count: infinite;
  }

  @keyframes shake {
    0% {
      transform: translate(1px, 1px) rotate(0deg);
    }
    10% {
      transform: translate(-1px, -2px) rotate(-1deg);
    }
    20% {
      transform: translate(-3px, 0px) rotate(1deg);
    }
    30% {
      transform: translate(3px, 2px) rotate(0deg);
    }
    40% {
      transform: translate(1px, -1px) rotate(1deg);
    }
    50% {
      transform: translate(-1px, 2px) rotate(-1deg);
    }
    60% {
      transform: translate(-3px, 1px) rotate(0deg);
    }
    70% {
      transform: translate(3px, 1px) rotate(-1deg);
    }
    80% {
      transform: translate(-1px, -1px) rotate(1deg);
    }
    90% {
      transform: translate(1px, 2px) rotate(0deg);
    }
    100% {
      transform: translate(1px, -2px) rotate(-1deg);
    }
  }

  @media (max-width: 930px) {
    .topnav {
      height: 140px;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      border-bottom: none;
    }

    ul {
      padding: 0;
    }

    li {
      margin: 0 15px;
    }
  }
</style>
